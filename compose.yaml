services:
  web:
    build:
      context: .
      dockerfile: Containerfile
    environment:
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-https://umg-api-django.onrender.com}
    ports:
      - "3000:3000"
    restart: unless-stopped
