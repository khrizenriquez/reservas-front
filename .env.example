# Base URL for the only supported backend: Render v1.
NEXT_PUBLIC_API_BASE_URL=https://umg-api-django.onrender.com
# The only supported API profile. This value is public and contains no credential.
NEXT_PUBLIC_API_PROFILE=render-v1
