import {fireEvent,render,screen,waitFor} from "@testing-library/react";
import Page from "./page";
import { LanguageProvider } from "@/components/LanguageProvider";
const getLabAvailability=jest.fn();jest.mock("@/services/render-api",()=>({createRenderApiClient:()=>({getLabAvailability})}));
describe("availability",()=>{const search=async()=>{fireEvent.change(screen.getByLabelText("Fecha"),{target:{value:"2026-08-15"}});fireEvent.change(screen.getByLabelText("Hora de inicio"),{target:{value:"08:00"}});fireEvent.change(screen.getByLabelText("Hora de fin"),{target:{value:"09:00"}});fireEvent.click(screen.getByRole("button",{name:"Buscar disponibilidad"}));};it("queries Render with date and interval",async()=>{getLabAvailability.mockResolvedValue([{id:1,name:"A1"}]);render(<Page/>);await search();await waitFor(()=>expect(getLabAvailability).toHaveBeenCalledWith({fecha:"2026-08-15",hora_inicio:"08:00",hora_fin:"09:00"}));expect(screen.getByLabelText("Laboratorios disponibles")).toHaveTextContent("A1");});it("shows an empty state",async()=>{getLabAvailability.mockResolvedValue([]);render(<Page/>);await search();expect(await screen.findByRole("status")).toHaveTextContent("No hay laboratorios");});it("shows a localized network error",async()=>{getLabAvailability.mockRejectedValue(new Error("offline"));render(<LanguageProvider><Page/></LanguageProvider>);await search();expect(await screen.findByRole("alert")).toHaveTextContent("Revisa tu conexión");});});
